Vagrant.configure(2) do |config|
  config.vm.define "Machine1" do |subconfig|
		subconfig.vm.box = "generic/ubuntu1804"
  end
  
	config.vm.define "Machine2" do |subconfig1|
		subconfig1.vm.box = "generic/ubuntu1804"
  end
end